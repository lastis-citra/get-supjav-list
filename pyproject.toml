[project]
name = "get-supjav-list"
requires-python = ">=3.14"
dynamic = ["version"]
dependencies = [
    "requests",
    "beautifulsoup4",
    "cloudscraper",
    "selenium",
    "webdriver-manager",
]

[tool.setuptools]
py-modules = []
